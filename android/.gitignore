# Local Android SDK location, machine specific.
local.properties
# Build output and Gradle's own caches.
.gradle/
build/
app/build/
*.apk
*.aab
# Signing material never belongs in the repository.
*.jks
*.keystore
keystore.properties

# Native build scratch
app/.cxx/

# Built from dist/ by make-payload.js on every build; not a source file.
app/src/main/assets/engine.zip
